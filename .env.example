# Copy to .env and fill in for real runs against an OpenAI-compatible API.
# Never commit the real .env file (already covered by .gitignore).

OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-replace-me
MODEL=gpt-4o-mini
